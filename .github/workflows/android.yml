name: Android CA
on: 
push:
    branches:
      - main
pull_request:
    branches:
      - main


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

	 - name: Set up JDJ
      uses: actions/setup-java@v4
	     with:
        java-version: '17'
        distribution: temurin 
		 - name: Generate Gradle Wrapper
      uses: gradle-wrapper-action@c1.5.2
	    with:
	        gradle-version: 8.4
		 - name: Build APK
      run: ./gradlew assembleDebug --no-daemon
		 - name: Upload APK,
      uses: actions/upload-artifact@vc
	    with:
        name: app-debug
        path: app/build/outputs/apk/debug/*.apk#