package com.example.simplereader.utils.input;

public class TouchMonitor {
    private boolean isTouching = false;
    public boolean isTouching() { return isTouching; }
    public void setTouching(boolean touching) { this.isTouching = touching; }
}
